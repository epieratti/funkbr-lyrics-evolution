###############################################################################
# FUNKBR-LYRICS-EVOLUTION (2005–2025)
# Arquivo modelo de variáveis de ambiente (.env.example)
# Este arquivo NÃO contém segredos. Serve apenas como referência de estrutura.
###############################################################################

##############################
# 🔑 CREDENCIAIS DE API
##############################

# Spotify API (obrigatório)
SPOTIFY_CLIENT_ID="coloque_aqui_o_client_id"
SPOTIFY_CLIENT_SECRET="coloque_aqui_o_client_secret"

# Genius API (para letras — usado na Fase 4)
GENIUS_API_TOKEN="coloque_aqui_o_token"

# Vagalume API (fallback para letras)
VAGALUME_API_KEY="coloque_aqui_o_api_key"

##############################
# ⚙️ PARÂMETROS GERAIS DE COLETA
##############################

# Intervalo de anos do estudo
YEAR_START=2005
YEAR_END=2025

# Mercado-alvo (BR = Brasil)
MARKET="BR"

# Incluir artistas relacionados (1 = sim, 0 = não)
INCLUDE_RELATED=1
MAX_RELATED_PER_SEED=20

# Caminho do arquivo de seeds (lista de artistas)
SEED_FILE="seed_artists.txt"

# Saídas de coleta
OUTPUT_JSONL="funk_br_discografia_raw.jsonl"
OUTPUT_CSV="funk_br_discografia_2005_2025.csv"

##############################
# 🧠 LIMITES E RETENTATIVAS
##############################

# Limites de chamadas por segundo
RATE_LIMIT_SPOTIFY=1
RATE_LIMIT_GENIUS=1
RATE_LIMIT_VAGALUME=1

# Espera entre chamadas (em segundos)
SLEEP_BETWEEN_CALLS=0.2

# Tempo máximo de resposta (em segundos)
TIMEOUT_S=120

# Backoff exponencial inicial (em segundos)
BACKOFF_START=1.5

# Máximo de tentativas em falhas transitórias
MAX_RETRIES=5

##############################
# 💾 GRAVAÇÃO E LOGS
##############################

# Quantas linhas gravar antes de flush parcial
FLUSH_EVERY_N_ROWS=500

# Arquivos de progresso e log
PROGRESS_FILE="collector_progress.json"
LOG_FILE="collector_log.txt"

# Ativar debug detalhado (1 = sim, 0 = não)
DEBUG=0

##############################
# 🧩 ESTRUTURA DE DIRETÓRIOS
##############################

# Pastas principais (mantidas conforme protocolo)
DATA_DIR="./data"
LOG_DIR="./logs"
REPORT_DIR="./reports"
CODE_DIR="./code"

##############################
# 🖥️ EXECUÇÃO / VM / CRON
##############################

# Nome do projeto (para logs e snapshots)
PROJECT_NAME="funkbr-lyrics-evolution"

# Caminho base do repositório na VM
PROJECT_PATH="/home/enrico/projeto"

# Ativar execução noturna via cron (1 = ativo, 0 = desativado)
ENABLE_CRON=1

# Limites de CPU/RAM para coleta (sugestão de monitoramento)
CPU_LIMIT_PERCENT=90
MEMORY_LIMIT_GB=14

##############################
# 🧮 VARIÁVEIS DE PROCESSAMENTO DE TEXTO
##############################

# Limiar mínimo de idioma português por linha (0–1)
LANG_THRESHOLD_PT=0.65

# Tamanho mínimo de letra (tokens)
MIN_TOKENS_LETRA=50

# Remover duplicatas de versos (1 = sim, 0 = não)
DEDUP_CHORUS=1

##############################
# 🧾 METADADOS E VERSÃO
##############################

# Versão do schema de dados
SCHEMA_VERSION="1.0"

# Tag Git correspondente
GIT_TAG="v1.0.0"

###############################################################################
# Fim do arquivo. Copie para .env e preencha suas chaves reais antes de rodar.
###############################################################################
